{
  "name": "reels-boost",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/dist/main.js",
    "build": "cd backend && npm install && npm run build"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
